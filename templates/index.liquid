<div id="index" class="clearfix">
  <div class="slider-container">
    <ul id="slider">
      <li class="slide"><img src="{{ 'slideshow_1.jpg' | asset_url }}" data-url="{{ settings.image_1_link }}" class="slide-img" /></li>
      <li class="slide"><img src="{{ 'slideshow_2.jpg' | asset_url }}" data-url="{{ settings.image_2_link }}" class="slide-img" /></li>
      <li class="slide"><img src="{{ 'slideshow_3.jpg' | asset_url }}" data-url="{{ settings.image_3_link }}" class="slide-img" /></li>
      <!-- <li class="slide"><img src="{{ 'slideshow_4.jpg' | asset_url }}" data-url="{{ settings.image_4_link }}" class="slide-img" /></li> -->
    </ul>
  </div>
  
  <div id="content-table" class="clearfix">
    {% for product in collections['all-products'].products %}
      {% include 'product-loop' with 'all-products' %}
    {% endfor %}
  </div>
<!--
  <div id="thirds-container" class="clearfix">
    <div id="home-blog-latest" class="home-third">
      <div class="home-heading"><a href="{{blogs.blog.url}}">Recent Blog Posts</a></div>
      {% for article in blogs.blog.articles limit:2 %}
      <div class="blog-title"><a href="{{ article.url }}">{{ article.title }}</a></div>
      <div class="blog-meta">{{article.author}} | {{ article.created_at | date: "%m.%d.%y" }} | <a href="{{ article.url }}#disqus_thread" data-disqus-identifier="{{article.id}}"></a></div>
      {{ article.content | strip_html | truncate: 200}}
      {% endfor %}
    </div>
    <div id="home-featured-product" class="home-third">
      <div class="home-heading" id="collection-heading"><a href="/products/classic-krapy-hoody">Classic Krapy Pullover Hoody</a></div>
      <a href="/products/classic-krapy-hoody"><img src="{{ 'hoodie_290.png' | asset_url}}" id="home-featured-img" /></a>
      <div id="premiere">
        <a id="shop-now" href="/products/classic-krapy-hoody">Shop Now!</a>
      </div>
    </div>
    <div id="home-twitter-widget" class="home-third">

    </div>
  </div>
 -->

</div> <!-- /#index -->